{
  "name": "bavis-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd bavis-frontend && npm install && npm run build",
    "start": "cd bavis-frontend && npm run preview"
  }
}
