{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd bavis-frontend && npm install && npm run build",
  "outputDirectory": "bavis-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
